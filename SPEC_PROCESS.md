# PawPal 规格与计划过程记录

## 使用方法与冷启动验证

项目使用 Superpowers 工作流：`brainstorming` → `writing-plans` → worktree → `subagent-driven-development` → TDD → 评审与验证。早期设计和计划保留在 `docs/superpowers/`。

在实现前，使用未携带主会话历史的不同智能体进行冷启动审查。其只读取规格和计划，指出会话生命周期、邀请令牌、主人不能被移除等隐含规则不够明确。随后在早期规格中补充：会话有效期、邀请仅保存摘要、邀请邮箱绑定、最后一名主人不可移除，以及并发接受邀请仅允许一次成功。这次审查证明“看起来清楚”的产品描述仍不足以直接实现。

## 三轮关键迭代

### 迭代一：从个人日记到共同照护

初始想法是“宠物成长记录”。brainstorming 追问“谁会共同使用、如何避免重复照护”，促使项目改为多宠物家庭的共同照护工具。采纳了“今日任务 + 成长瞬间 + 宠物档案”三模块，因为它们在没有外部 API 的条件下仍构成可演示闭环。

### 迭代二：安全协作设想与范围控制

AI 建议角色、邀请和短期交接模型；学生确认主人、协作照护者和临时照护者的边界，并排除诊断、支付和社交功能。后续实现环境无法可靠获取 Prisma/PostgreSQL 依赖，因此没有把未完成的认证/邀请伪装成已完成能力，而是将发布规格收敛到实际交付的本地演示版。

### 迭代三：从关系数据库改为 JSON 演示存储

依赖下载持续失败时，AI 建议改用 Node 标准库 JSON Store，并以“临时文件 + rename”保证单文件写入不被中断。学生接受该替代方案，因为它保留了宠物、任务和成长记录的核心体验，并明确写入单进程、非生产限制。该变更记录在 `docs/superpowers/specs/2026-08-12-json-persistence-fallback-design.md`。

## TDD 与评审证据

- T1 先创建健康检查失败测试，再实现路由；过程提交在 `bf51784`。
- T2 先创建 JSON 原子写入和任务不能重复完成的测试，再实现 Store/Repository；过程提交在 `d0b279d`。
- 每个任务完成后先对照规格检查范围，再检查代码的错误处理、数据写入和可读性。

## 反思：流程的价值与局限

brainstorming 最有价值的部分是不断追问用户、角色和边界；它使项目从“页面”变成了有明确使用情境的工具。其不足是早期规格容易比可用时间和环境能力更大，必须通过冷启动审查和最终规格收敛避免“文档很完整、代码却没有实现”的错觉。对本项目而言，最重要的人工判断是承认环境约束并如实缩小承诺，而不是继续堆叠未验证功能。
