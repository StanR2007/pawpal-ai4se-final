# PawPal｜多人协作宠物成长与照护平台

## 产品目标

PawPal 让家人、情侣与室友共同管理多只宠物的照护任务、成长记录、健康档案和临时交接，减少重复照护与信息断层。它不提供诊断或治疗建议。

## 已确认范围

- 一个账户可创建或加入多个家庭；一个家庭可管理多只宠物。
- 角色为主人、协作照护者、临时照护者。主人管理家庭、成员与所有宠物；协作照护者只读写被授权宠物的日常内容；临时照护者仅能在有效交接期访问指定资源。
- 功能模块：宠物档案与图片时间线、共享任务与站内提醒、健康记录与就诊摘要、限时临时交接、审计日志。
- UI 温暖轻松、移动优先；奶油白、鼠尾草绿、暖橙为基础色，风险状态必须有文本和图标而非仅颜色。
- 首版不做社交、支付、在线问诊、视频、图像识别及外部通知。

## 技术与交付约束

- Next.js + TypeScript 单体全栈；PostgreSQL + Prisma；Docker Compose；GitLab CI；Vitest、Testing Library 与 Playwright。
- 使用 Argon2id 保存密码哈希；使用 HttpOnly、SameSite=Lax 会话 Cookie；生产环境 Cookie 必须 Secure。
- 生产图片使用 S3 兼容对象存储，开发使用挂载目录。所有上传均校验格式、文件签名、大小和家庭权限。
- 应提供 `make test`、Docker 单命令启动、GitLab CI 的 `unit-test` job、公开可访问 Web UI 和 README 部署说明。

## 认证与会话

- 邮箱 `trim`、规范化并转小写后全局唯一；密码长度 12–128 字符。首版不支持邮箱验证、改邮箱、改密码或密码重置。
- CSPRNG 生成至少 256-bit 会话秘密；Cookie 保存原值，数据库只保存 SHA-256 摘要。会话绝对有效期 30 天，闲置 7 天失效；每账户最多 5 个会话，超限撤销最早会话。
- 所有写请求校验同源 Origin。未认证返回 401；已认证却无家庭/宠物权限返回 404。登录失败与账户不存在同为 401 通用消息；重复注册为 409。

## 邀请、角色与授权

- 仅主人可创建/查看/撤销待接受邀请；邀请仅能授予协作照护者或临时照护者，不能授予主人。
- 邀请绑定规范化目标邮箱，令牌为 256-bit CSPRNG 且数据库仅存摘要；只能被对应已登录账户接受。接受期限 1 小时至 30 天，使用服务端 UTC 时间；链接设 `Referrer-Policy: no-referrer` 且令牌不写日志。
- 接受邀请以事务条件更新：仅待接受、未撤销、未过期邀请可接受；并发时仅一个请求返回 201，其余返回 409。`Membership(householdId, accountId)` 唯一。
- 家庭始终至少有一名主人。仅主人可管理成员；只能提升已激活的协作照护者；会使家庭没有主人的移除、降级或离开操作返回 409。
- 协作照护宠物范围是 `ALL` 或非空 `SELECTED`；`ALL` 包含未来新增宠物。临时成员在有效交接单开始前没有家庭、宠物、任务或健康记录访问权。

## 后续模块验收摘要

- 同一任务并发完成时只能有一个成功记录；固定、轮换、自由认领和逾期看板均可用。
- 成员能记录 JPG/PNG/WebP 成长图片和七类健康记录；主人能归档健康记录并导出指定日期的就诊摘要。
- 交接单指定临时成员、宠物、有效期、任务和注意事项；到期或撤销后立即拒绝访问。

完整设计过程与冷启动审查记录保留在项目交付附件中；本文件是实现的规范性来源。
