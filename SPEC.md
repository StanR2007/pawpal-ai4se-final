# PawPal｜多人宠物成长与照护记录（课程演示版）

## 1. 问题与用户

家庭、情侣或室友共同照护宠物时，喂食、散步和成长照片常分散在聊天记录中，容易重复照护或遗漏重要瞬间。PawPal 为小型多宠物家庭提供一个可立即打开的共用记录页，集中展示宠物、当日照护和成长瞬间。

目标用户是 2–5 人共同照护 1–5 只宠物的小型家庭。本项目不提供医疗诊断、在线问诊或付费服务。

## 2. 用户故事

1. 作为照护者，我想查看家庭已有宠物，以便快速了解成员。
2. 作为照护者，我想新增宠物，以便把新成员纳入记录。
3. 作为照护者，我想查看待办照护任务，以便避免遗忘。
4. 作为照护者，我想完成一次任务且不能重复完成，以便记录可信。
5. 作为照护者，我想为某只宠物记录成长瞬间，以便保存回忆。

## 3. 功能规约与验收

| 模块 | 输入 / 行为 | 输出、边界与验收 |
| --- | --- | --- |
| 宠物档案 | 名称、品种、表情；名称和品种必填 | 新宠物显示在家庭卡片中；缺失必填字段由浏览器阻止提交。 |
| 今日照护 | 读取演示家庭任务；点击完成 | 第一次完成后状态为 `COMPLETED`；再次请求返回冲突错误，不能重复完成。 |
| 成长记录 | 宠物、标题、描述均必填 | 新记录显示在时间线，并持久化到本地数据文件。 |
| 演示数据 | 首次访问无数据文件 | 自动建立“阳光小屋”、Momo、豆豆、三项任务与两条记录。 |

## 4. 非功能性与安全

- 性能：本地演示数据量（5 只宠物、100 条记录）下页面操作应为即时交互。
- 可用性：移动优先布局；状态同时使用文字和符号表达。
- 数据一致性：JSON 写入先写临时文件，再原子重命名；单进程队列保证同一任务只有一次完成。
- 安全边界：本版本不调用付费 API、不收集密码或 API Key，因此无用户凭据需要保存。严禁将 `.env`、真实隐私数据或访问令牌提交到仓库。
- 可观测性：`GET /api/health` 返回服务状态，供部署平台检查。

## 5. 架构与数据模型

```text
Next.js 页面 / API Routes → Repository → JSON Store → data/pawpal.json
```

主要实体为 `Household`、`Pet`、`CareTask`、`GrowthEvent`。每只宠物属于一个家庭；任务和成长记录均关联家庭与宠物。JSON Store 是唯一文件访问层，未来可替换为关系数据库实现。

## 6. 技术选型、分发与部署

- TypeScript + Next.js：一个仓库同时实现页面与 API，类型清晰，适合课程演示。
- Vitest：提供可一键执行的核心单元测试。
- Docker Compose：本地容器启动，并以卷保存 `data/`。
- Vercel：公开 Web UI 地址为 <https://pawpal-ai4se-final.vercel.app>。
- UI 采用自定义的暖色移动优先设计；未使用 Open Design，原因是此演示版以极简仪表盘为范围，使用自定义 CSS 可保持依赖最少。

分发方式为公开源代码 + Docker Compose。目标机器需 Docker 或 Node.js 22+ 与 pnpm；本版本没有 API Key 配置步骤。已知限制是 JSON 存储仅适合单进程演示，线上免费实例不应用于保存真实或敏感数据。

## 7. 风险与未决事项

- 没有账号、邀请、跨设备协作、图片上传、健康档案或临时交接；这些明确不属于本次完成范围。
- JSON 数据不支持多实例写入；生产化需要 PostgreSQL、身份认证与对象存储。
- Vercel 的无状态运行环境不保证 JSON 文件持久化，线上站点用于功能展示，本地 Docker 演示用于验证持久化。
