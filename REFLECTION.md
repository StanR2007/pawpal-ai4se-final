# PawPal 期末反思报告

> **学号：251250071｜姓名：刘睿祺**

## 1. 项目概述

PawPal 解决的是多人照护宠物时信息分散的问题：喂食、散步等事项常散落在聊天记录里，难以确认是否已完成，也容易遗漏成长瞬间。目标用户是共同照护宠物的小型家庭、情侣或室友，需要一个能看清宠物、今日待办和近期记录的共享页面。最终交付包含三个核心模块：可新增并展示的宠物档案；可查看、完成且不能重复完成的今日照护；可为指定宠物新增、并保存到本地文件的成长记录。首次访问会生成“阳光小屋”、Momo、豆豆和演示数据。

与最初多人协作的设想相比，我主动将范围收敛为本地单进程 JSON 演示版，未实现账号、邀请、跨设备协作、图片上传、健康档案和临时交接。依赖下载受限使 Prisma/PostgreSQL 无法可靠落地；继续保留这些承诺会让文档比代码走得更远。收敛后仍保留可演示、可测试的闭环。

## 2. Superpowers 工作流的实际作用

`brainstorming` 中最改变我想法的一次追问是“谁会共同使用，以及如何避免重复照护”。最初我把项目理解为个人宠物成长日记，重点在照片和文字；这次追问让我认识到关键矛盾是共同照护者之间的状态不同步。因此项目变为“今日任务 + 成长瞬间 + 宠物档案”的小型共享工具，“任务只能完成一次”也成为可验收的业务规则。

`writing-plans` 将任务拆为可交付单元：T1 搭建运行、测试与 CI 基础；T2 实现 JSON 原子写入、种子数据和任务互斥；T3 连接 API 与界面；T4 整理规格和交付材料。这样先验证基础，再处理数据一致性；隔离 worktree 避免试验污染主目录，subagent 工作流则迫使我写清输入、输出和验收条件。

我认为最有价值的是“先写清边界再实现”。发现数据库依赖不可用后，我没有把未完成的认证和邀请伪装成能力，而是同步收敛规格与计划。固定流程也有形式感：计划完整不等于环境可用。下次应更早验证依赖安装和构建。

## 3. TDD、测试与评审

本项目有两次真实的“红—绿”经历。T1 先创建健康检查路由的单元测试；路由未实现时，测试无法取得预期响应。随后只实现最小的 `GET /api/health`，测试转绿。T2 则先为 JSON 原子写入和“同一任务不能重复完成”写测试。实现前没有写入互斥和状态判断；最小实现是以单进程队列处理写入，先写临时文件再原子重命名，并在任务已为 `COMPLETED` 时返回冲突错误，之后再整理读取和错误处理。

AI 协作下，TDD 对我更像放大器。AI 能快速生成候选实现，也会放大“看起来合理”的错误；测试把讨论从“代码像不像”变为“用户故事是否成立”。评审时我修正了只依赖前端禁用按钮的想法：重复请求仍可能发生，规则必须放在 Store/Repository 层。测试通过也不等于需求完成，还要对照 SPEC、构建结果和手动演示核验。

## 4. 规格、冷启动与任务粒度

冷启动审查暴露的主要是规格问题，而不是模型本身的问题。早期描述中有多人协作、会话、邀请和角色，却没有写清会话有效期、邀请令牌保存方式、邮箱绑定、最后一名主人能否移除，以及并发接受邀请的结果。没有主会话上下文的智能体会在这些地方暂停或猜测，说明“看起来完整”的产品描述并不能直接实现。

据此，早期规格补充了会话生命周期、邀请仅保存摘要、邮箱绑定、最后一位主人不可移除和邀请只成功一次等规则。后来数据库方案无法可靠实施时，我将当前 SPEC 收敛为 JSON 演示版，把认证和邀请列为未完成项。合适的任务粒度应是一个可独立验收的纵向能力：T2 的原子写入、任务互斥和种子数据可以由 `store.test.ts` 验证；若将全部多人协作与数据库迁移合成一个任务，就会难以定位失败原因。SPEC 提供边界与验收条件，PLAN 把它们映射为实现顺序和测试证据。

## 5. 工程交付：安全、分发与部署

当前版本不收集真实密码、不调用付费 API，也不保存 API Key；README 与 SPEC 明确要求不提交 `.env`、访问令牌和真实隐私数据。这与演示范围匹配：没有认证和密钥管理，就不制造不可验证的安全机制。

本地 `pnpm dev` 适合开发与演示；Docker Compose 提供一致的运行环境，并将 `data/` 挂载为持久化卷，用于验证 JSON 写入；Vercel 提供公开 UI 演示，但无状态环境不能保证 JSON 长期保存，故线上仅用于功能展示，持久化应在本地 Docker 验证。JSON Store 以临时文件加原子重命名降低中断风险，并以单进程队列保护任务完成；但它不适合多实例并发和真实多用户生产数据，后续需要 PostgreSQL、认证与对象存储。CI 的困难是 Windows 工作树中的 pnpm/Vitest 链接和 npm registry 下载限制；处理方式是由 CI 从锁文件重新安装，再运行测试和生产构建，并如实记录环境限制。

## 6. 批判与重做计划

Superpowers 隐含的前提是依赖可获得、技术栈可安装、各阶段有稳定验证环境。本项目中，“先澄清用户与边界、再拆分任务、用测试验证规则”成立；早期 PostgreSQL/Prisma 能顺利落地则不成立。工程责任在于及时发现差异，而非为了遵守计划交付无法运行的配置。

如果重做，我会优先：第一，在完整数据库设计前验证安装、构建和最小连接；第二，将“本次必须演示”与“生产化路线图”分层；第三，更早验证 Vercel 与本地持久化的区别。最有效的 prompt/context 策略是同时提供用户故事、不可做的范围、验收标准和环境限制，而不是只说“实现某功能”。

这次项目让我认识到，AI 时代工程师的责任更集中在判断和验证：定义问题、识别假设、审查方案，并如实说明证据与限制。PawPal 不是完整的生产协作平台，但它是一个能运行、能测试且清楚说明不能做什么的演示交付；我认为这种诚实的工程边界本身就是质量的一部分。

## AI 辅助声明

本文观点、项目经历和案例由本人根据项目文档、代码提交和测试记录整理确认；AI 的使用范围为协助梳理段落结构与进行语言润色，未将无法核对的功能或经历写入正文。提交前由本人复核并按实际情况修改。
